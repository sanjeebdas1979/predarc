# Default is testnet. Set mainnet only after the full workflow is verified.
NEXT_PUBLIC_PREDARC_DAILY_NETWORK=testnet
# Address of a NEW PredarcDaily deployment on the selected chain. Never use V2.
NEXT_PUBLIC_PREDARC_DAILY_ADDRESS=
# Keep false until server-validated prediction acceptance/settlement is connected.
NEXT_PUBLIC_PREDARC_DAILY_ENABLED=false
